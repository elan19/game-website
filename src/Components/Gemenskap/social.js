import React, { useState } from 'react';
import styles from './Social.module.css'; // Create a CSS module for styling

const Social = () => {

    return (
        <div className={styles.socialBackground}>
        </div>
    );
};

export default Social;
